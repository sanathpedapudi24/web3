# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Status

This repository is currently **empty**. No source code, build configuration, tests, or documentation exist yet. The git history shows:
- A first commit with a README.md titled "web3"
- A subsequent commit that deleted the README

The repository name and initial commit suggest this is intended to be a Web3/blockchain-related project, but no implementation is present.

## Development Setup

There is no existing build system, testing framework, or project structure. When code is added, ensure this file is updated with:
- Commands to install dependencies
- Build and lint commands
- Test execution commands (including how to run single tests)
- Architecture overview and project structure

## No Existing Rules

No CLAUDE.md existed previously. No Cursor rules (.cursor/rules/) or Copilot instructions (.github/copilot-instructions.md) are present.
